# دليل إعداد حساب المدير 👨‍💼

## الطريقة الأولى: عبر واجهة Supabase (الأسهل)

### الخطوات:

1. **افتح لوحة تحكم Supabase**
   - اذهب إلى: https://supabase.com/dashboard
   - اختر مشروعك

2. **إنشاء المستخدم**
   - اذهب إلى: `Authentication` → `Users` → `Add User`
   - أدخل البيانات التالية:
     - **Email**: `mus5667@gmail.com`
     - **Password**: `123456`
     - ✅ تأكد من تفعيل: `Auto Confirm User`
   - اضغط `Create User`

3. **منح صلاحيات المدير**
   - اذهب إلى: `SQL Editor` → `New Query`
   - انسخ والصق الأمر التالي:
   ```sql
   UPDATE public.profiles 
   SET is_admin = true 
   WHERE email = 'mus5667@gmail.com';
   ```
   - اضغط `Run`

4. **تأكد من النجاح**
   ```sql
   SELECT email, is_admin 
   FROM public.profiles 
   WHERE email = 'mus5667@gmail.com';
   ```
   - يجب أن ترى: `is_admin = true`

---

## الطريقة الثانية: عبر واجهة التطبيق

### الخطوات:

1. **افتح صفحة التسجيل**
   - افتح: `auth.html` في المتصفح

2. **سجل بالبيانات التالية**
   - Email: `mus5667@gmail.com`
   - Password: `123456`

3. **منح صلاحيات المدير عبر SQL**
   - افتح Supabase SQL Editor
   - شغل:
   ```sql
   UPDATE public.profiles 
   SET is_admin = true 
   WHERE email = 'mus5667@gmail.com';
   ```

---

## ✅ تسجيل الدخول كمدير

بعد إكمال الخطوات، يمكنك تسجيل الدخول:

- **الصفحة**: `admin.html`
- **Email**: `mus5667@gmail.com`
- **Password**: `123456`

---

## ⚠️ ملاحظات أمنية

- غيّر كلمة المرور بعد أول تسجيل دخول
- لا تشارك بيانات الاعتماد هذه مع الآخرين
- استخدم كلمة مرور قوية في الإنتاج (Production)
